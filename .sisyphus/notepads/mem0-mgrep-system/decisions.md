# Decisions
- Separate corpora: memories vs file/doc corpus (never mix collections)
- V1 scope: code + Markdown only
- NO synthesized answers in v1
- MCP integration: HTTP-backed bridge
- Reset/drop: file corpus only, never touches memory corpus
- No commits or pushes unless user explicitly commands
